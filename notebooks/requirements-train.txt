# Training environment for WeldMind Qwen3-VL-4B notebook.
# Target: 1× NVIDIA H20-NVLink, CUDA 12.4, sm_90.
#
# Install order (flash-attn needs torch present first):
#   pip install -r requirements-train.txt
#   pip install flash-attn==2.6.3 --no-build-isolation
#
# If you're on a different CUDA version, drop the +cu124 suffix and the
# extra-index-url, and install torch matching your CUDA from
# https://pytorch.org/get-started/locally/

--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.4.1+cu124
torchvision==0.19.1+cu124

transformers>=4.49,<4.52
modelscope>=1.25
peft>=0.13
accelerate>=1.0
datasets>=3.0
qwen-vl-utils>=0.0.10

# Optional fallback if memory ever pinches.
bitsandbytes>=0.45

pillow>=10.0
scikit-learn>=1.3
pandas>=2.0
matplotlib>=3.7
tqdm>=4.65
numpy>=1.26

# Notebook UI.
jupyterlab>=4.0
ipywidgets>=8.0
nbformat>=5.9
